# 0.16.17

- Stable Audio 3 Medium 默认启用 `--model-half` 半精度模式，降低显存占用，并自动迁移旧版启动参数。
- 启动失败详情会识别 Windows 原生访问冲突 `0xC0000005`，说明 CUDA、PyTorch 原生扩展和显卡驱动等常见原因及处理建议。
- 负数进程退出码会同时显示十进制与十六进制，便于定位 Windows 原生异常。
- 卸载插件前会扫描并终止仍在使用插件目录的残留进程树，等待进程完全退出后再移动目录。
- 插件目录遇到杀毒扫描、资源管理器或索引器造成的短暂文件锁时会自动重试；持续占用时会显示可操作的处理提示。
