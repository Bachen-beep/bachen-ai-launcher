# 0.16.13

- 修复从 GitHub 自动导入 Stable Audio 3 后只保留安装时所选档位的问题；安装一次即可在 Small SFX、Small Music、Medium 三种启动配置间切换。
- 保留安装确认时选择的初始档位，切换其他档位时按对应 Hugging Face 门控仓库独立验证权限并在首次启动时下载模型。
- 修复运行日志抽屉动画同时逐帧调整面板和窗口边界导致的抖动；窗口仅调整一次，动画期间阻止重复切换，收起完成后再隐藏内容。
