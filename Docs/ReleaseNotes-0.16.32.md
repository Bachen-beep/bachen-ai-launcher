# 0.16.32

- 修复启动器更新下载过程中出现 `unexpected EOF` 或传输流提前结束时立即失败的问题。
- 更新包下载现在保留 `.partial` 文件，自动重试最多 5 次，并使用 HTTP Range 从已下载位置继续。
- 已知文件长度但收到提前 EOF 时会自动恢复；重试耗尽后显示代理、防火墙和网络排查建议。
- 新增断流重试、Range 续传和临时文件最终化回归测试。
