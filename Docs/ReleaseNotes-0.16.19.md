# 0.16.19

- 稳定版更新检查优先并行读取经过 RSA 验签的 GitHub Raw 稳定索引和 `releases/latest` 直连清单；只要直连来源有效，就不再请求受匿名额度限制的 GitHub Release API。
- 仅当全部直连来源失效，或直连版本低于本机曾验证过的最高版本时，才把 GitHub API 作为最后回退来源。
- 预览通道遇到 GitHub API `403/429` 限流或网络错误时，会自动降级到稳定版签名清单，不再弹出阻断式“启动器更新失败”。
